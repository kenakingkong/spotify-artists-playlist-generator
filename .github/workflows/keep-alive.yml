name: Keep Supabase Alive
on:
  schedule:
    - cron: '0 0 * * 0'  # every Sunday at midnight

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl https://setlists.makenakong.com/api/ping